## Codebase Patterns
- Use Node.js route handlers and server-only modules for all pi SDK integration.
- Keep generated audio under `.data/audio` and expose it through `/api/audio/[id]` instead of direct file paths.
- Model/TTS configuration should come from env vars with safe defaults and friendly runtime errors.
- Configure `serverExternalPackages` for pi SDK packages so Next.js does not try to bundle Node-heavy agent dependencies.
- Create per-session workspaces under `.data/sessions/<sessionId>/workspace` and keep session records beside them for isolated pi conversations.

## 2026-04-09 06:58 UTC - US-001: Project foundation and session skeleton
- Added pi SDK dependencies, a `typecheck` script, and Next.js server external package config for `@mariozechner/pi-coding-agent`.
- Implemented server-only pi foundation modules for environment/model discovery, repo-local data paths, and an in-memory session store backed by real `createAgentSession()` instances.
- Added `POST /api/session` and `POST /api/chat` Node route handlers with friendly error handling and session concurrency protection.
- Replaced the starter homepage with a product shell that documents the backend contract and environment readiness.
- Files changed: `.gitignore`, `package.json`, `package-lock.json`, `next.config.ts`, `src/app/layout.tsx`, `src/app/globals.css`, `src/app/page.tsx`, `src/app/api/session/route.ts`, `src/app/api/chat/route.ts`, `src/server/pi/paths.ts`, `src/server/pi/services.ts`, `src/server/pi/session-store.ts`.
- **Learnings for future iterations:**
  - Keep pi auth/model lookup lazy and refreshable so the server can pick up `/login` changes without a reboot.
  - Count tool executions from session events instead of trying to parse assistant text.
  - A single-process in-memory store is sufficient for MVP, but later stories should keep the response contract structured for UI rendering and media attachments.
---

## 2026-04-09 08:01 UTC - US-002: Pi agent and Kokoro TTS tool
- Kept real `createAgentSession()` setup with pi's default coding tools and added exactly one custom `synthesize_speech` tool backed by Kokoro-FastAPI's OpenAI-compatible `POST /v1/audio/speech` endpoint.
- Added server-only Kokoro config and audio persistence modules so generated files land under `.data/audio` with per-audio JSON metadata instead of `public/`.
- Added `GET /api/audio/[audioId]` on the Node runtime to validate ids, read stored metadata, and serve audio bytes with the recorded content type.
- Updated `/api/chat` responses to include typed `audioItems` from the current turn while keeping the contract minimal for the later UI story.
- Refined the embedded system prompt so the pi agent knows to use speech synthesis only when the user explicitly asks to hear selected text.
- Updated the landing page copy to reflect the new backend contract.
- Files changed: `package.json`, `package-lock.json`, `prd.json`, `progress.txt`, `src/app/page.tsx`, `src/app/api/audio/[audioId]/route.ts`, `src/server/pi/audio-store.ts`, `src/server/pi/kokoro.ts`, `src/server/pi/paths.ts`, `src/server/pi/session-store.ts`.
- Validation: `npm run lint`, `npm run typecheck`, and `npm run build` all passed.
- **Follow-ups for US-003:**
  - Render `audioItems` cards in the chat transcript and play them through the new `/api/audio/[audioId]` route.
  - Add composer affordances for “read this aloud” requests and voice selection without forcing TTS into the default workflow.
  - Consider surfacing tool-level Kokoro failures inline in the UI when the agent chooses synthesis and the upstream service is unavailable.
---

## 2026-04-09 12:17 UTC - US-003: Listening practice chat UI
- Replaced the static homepage shell with a real client chat experience that auto-creates sessions, sends chat turns, shows loading/error states, and renders user/assistant transcript items.
- Added polished `audioItems` cards with inline `<audio controls>` playback, plus optional voice hints and starter prompts that help users start quickly without forcing any workflow.
- Moved browser-safe shared contracts into `src/lib/pi/contracts.ts` and added a typed frontend API client in `src/lib/pi/browser-client.ts` so the chat UI can stay strongly typed without importing server-only modules.
- Refined layout and global styles so the page feels like a product surface rather than a backend demo.
- Browser verification: opened the local app, confirmed session creation, sent a real prompt requesting spoken audio, received an assistant reply plus audio card, and verified the rendered playback route and transcript state. Screenshot saved at `/tmp/pi-listening-lab-ui.png`.
- Files changed: `src/app/page.tsx`, `src/app/_components/listening-lab-chat.tsx`, `src/app/layout.tsx`, `src/app/globals.css`, `src/lib/pi/contracts.ts`, `src/lib/pi/browser-client.ts`, `src/server/pi/audio-store.ts`, `src/server/pi/services.ts`, `src/server/pi/session-store.ts`.
- Validation: `npm run lint`, `npm run typecheck`, and `npm run build` all passed.
- **Learnings for future iterations:**
  - Keep server contracts shared in a frontend-safe module to avoid leaking `server-only` imports into client code.
  - For browser verification in dev mode, use `http://localhost:3000` rather than `127.0.0.1` to avoid Next.js dev-origin/HMR restrictions.
  - Returning `audioItems` directly from `/api/chat` keeps the UI simple and avoids a second fetch just to discover generated media.
---

## 2026-04-09 12:20 UTC - US-004: Developer setup documentation
- Replaced the placeholder README with developer-facing setup docs that describe the real product goal, App Router/server architecture, API routes, local run flow, and `.data` storage layout.
- Added a tracked `.env.example` with the app's meaningful Kokoro variables plus optional host-level pi provider key examples, and updated `.gitignore` so the example file stays versioned.
- Documented how pi model availability works on the host machine through `pi`, `/login`, `~/.pi/agent/auth.json`, `~/.pi/agent/models.json`, `PI_CODING_AGENT_DIR`, and provider environment variables rather than an app-managed auth screen.
- Added Kokoro-FastAPI local setup notes with the current upstream Docker image examples, quick endpoint checks, and a minimal curl smoke test for the app.
- Files changed: `.env.example`, `.gitignore`, `README.md`, `prd.json`, `progress.txt`.
- Validation: `npm run lint`, `npm run typecheck`, and `npm run build` all passed.
- **Learnings for future iterations:**
  - Keep developer docs aligned with the real runtime contract, especially when auth and model selection are delegated to host tooling rather than the app UI.
  - Track `.env.example` explicitly in `.gitignore` whenever the repo ignores `.env*`, or setup docs will drift from versioned defaults.
---
